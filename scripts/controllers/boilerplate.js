/**
* This is a description
*/
/*define([
	'jquery',
	'underscore',
	'can',
	'kendoweb',
    'app',
    'api',
    'utils/basecontrol'
], function ($, _, can, kendo, App, Api, Base) {

    //CACHE TO PREVENT POSSIBLE MEMORY LEAKS AND REBINDS
    return App.controllers.CONTROLLER_NAME || (App.controllers.CONTROLLER_NAME = new (Base({
        //INITIALIZE
        init: function (element, options) {
            //BASE INITIALIZE
            this._super(element, options);
        }
    }))(ELEMENT_SELECTOR)); //ROOT ELEMENT FOR CONTROLLER INSTANCE
});*/